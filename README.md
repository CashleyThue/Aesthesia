# Aesthesia (googled random words for this name)
password manager cli thingy thing

## TODO
- this thing prompts for password way too much lmao

## Usage
```bash
java -jar <path to the jar file built> <list/get/add/remove> <...>
```
add requires label, username and passwords as parameters
remove and get requires the target label
list requires none

again this thing prompts for password way too much.
